# skills/

Skills kept under version control here.

- **`adversarial-review/`** — a *personal* skill (not Wolfram-specific). Manual trigger
  only: invoke with `/adversarial-review` or by asking for a red-team pass on a
  proposal. Lives here for versioning and review; install it by saving the packaged
  `.skill` to your claude.ai profile, or by copying the directory into
  `~/.claude/skills/`.

Per `CLAUDE.md`, recurring *research* workflows also belong here as skills.
